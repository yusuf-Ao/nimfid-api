server:
  port: 8083

spring:
  application:
    name: notification-service
  zipkin:
    base-url: http://localhost:9411
  rabbitmq:
    addresses: localhost:5672
  mail:
    host: smtp.gmail.com
    port: 465
    username: feedback.aycodes@gmail.com
    password: imoacrdtnjtlkmtg
    properties:
      mail:
        transport:
          protocol: smtp
        debug: true
        smtp:
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
          auth: true
          ssl:
            enable: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8084/eureka

rabbitmq:
  exchanges:
    internal: internal.exchange
  queues:
    user-notification: user.notification.queue
  routing-keys:
    internal-user-notification: internal.user.notification.routing-key